import coords

type
  Quadrant = object
    klingon_count: Natural = 0
    starbase_count: Natural = 0
    star_count: Natural = 0

proc random_quadrant(): Quadrant =
  discard

type Galaxy
proc random_galaxy():

let quadrants = array[high(Coord), high[Coord],]

